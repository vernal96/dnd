# Frontend service

Сюда подключается клиентский UI с Canvas-рендером карты и обычными панелями интерфейса.

Ожидаемые переменные из compose:

- `VITE_API_URL=http://localhost:8080`
- `VITE_WS_URL=ws://localhost:8081`
